# TODO 6: Provide the application name for cloud config.
spring.application.name=event-service

# TODO 7: register the event-service for cloud config.
spring.cloud.config.uri=http://localhost:8888

# TODO 8: Btw. the bootstrap.properties or bootstrap.yml configuration is the minimal config you need to provide. They app can catch now all from cloud config server.

# TODO 9: Run all services they should all work. Btw. there is a MessageController in the event-service which listens to http://localhost:9090/message just try it.
